(library
 (name slice)
 (public_name diff_ppl.slice)
 (libraries gsl)
 (modules lat lats ast distributions lexer parser parse pretty interp util normalize discretization inference simplify cdf_ad adev))

(ocamllex lexer)

(menhir
 (modules parser))
