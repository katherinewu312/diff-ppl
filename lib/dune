(library
 (name slice)
 (public_name diff_ppl.slice)
 (libraries gsl)
 (modules lat lats ast distributions lexer parser parse pretty interp util cut_order normalize discretization inference simplify cdf_ad eval adev reverse))

(ocamllex lexer)

(menhir
 (modules parser))
